apiVersion: v1
kind: Service
metadata:
    name: jayshreeagsspringbootpipeline
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: jayshreeagsspringbootpipeline